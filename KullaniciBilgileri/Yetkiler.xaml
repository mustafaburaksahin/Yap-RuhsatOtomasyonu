<Page x:Class="YapıRuhsatOtomasyonu.KullaniciBilgileri.YetkilerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" d:DesignHeight="850" d:DesignWidth="1300"
      Title="Yetki Tanımlama" Background="#F0F0F0">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="420"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Yetki Detayları" BorderBrush="#D0D0D0" Margin="0,0,10,0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="135"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Text="ID:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <TextBox x:Name="txtId" Grid.Row="0" Grid.Column="1" IsReadOnly="True" Background="#E0E0E0" Margin="0,0,0,8" Padding="5"/>

                    <TextBlock Grid.Row="1" Text="Kullanıcı:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <ComboBox x:Name="cmbKullanici" Grid.Row="1" Grid.Column="1" Background="White" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2" Text="Yetki Tipi:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <ComboBox x:Name="cmbYetkiTipi" Grid.Row="2" Grid.Column="1" Background="White" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="3" Text="Modül:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <ComboBox x:Name="cmbModul" Grid.Row="3" Grid.Column="1" Background="White" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="4" Text="Panel:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <ComboBox x:Name="cmbPanel" Grid.Row="4" Grid.Column="1" Background="White" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="5" Text="Yetki:" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <CheckBox x:Name="chbYetki" Grid.Row="5" Grid.Column="1" Content="Evet" Margin="0,0,0,8" VerticalAlignment="Center"/>

                    <TextBlock Grid.Row="6" Text="Diğer Bilgiler:" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,5,8,8"/>
                    <TextBox x:Name="txtDigerBilgiler" Grid.Row="6" Grid.Column="1" Height="60" Background="White" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,8" Padding="5"/>

                    <TextBlock Grid.Row="7" Text="Açıklama:" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,5,8,15"/>
                    <TextBox x:Name="txtAciklama" Grid.Row="7" Grid.Column="1" Height="60" Background="White" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,15" Padding="5"/>

                    <TextBlock Grid.Row="8" Text="Kaydeden Kullanıcı:" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <ComboBox x:Name="cmbKaydeden" Grid.Row="8" Grid.Column="1" Background="White" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="9" Text="Kayıt Tarihi:" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <TextBox x:Name="txtKayitTarihi" Grid.Row="9" Grid.Column="1" IsReadOnly="True" Background="#E0E0E0" Margin="0,0,0,8" Padding="5"/>

                    <TextBlock Grid.Row="10" Text="Değiştiren Kullanıcı:" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <ComboBox x:Name="cmbDegistiren" Grid.Row="10" Grid.Column="1" Background="White" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="11" Text="Değiştirme Tarihi:" Foreground="Gray" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,8,8"/>
                    <TextBox x:Name="txtDegistirmeTarihi" Grid.Row="11" Grid.Column="1" IsReadOnly="True" Background="#E0E0E0" Padding="5" Margin="0,0,0,8"/>
                </Grid>
            </ScrollViewer>
        </GroupBox>

        <!-- PANEL 2: YETKİLER TABLOSU -->
        <GroupBox Grid.Column="1" Header="Yetkiler Listesi" BorderBrush="#D0D0D0" Margin="0">
            <DataGrid x:Name="dgYetkiler" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      Background="White" AlternatingRowBackground="#FAFAFA" SelectionChanged="dgYetkiler_SelectionChanged"
                      GridLinesVisibility="All" RowHeight="35">
                <DataGrid.Columns>
                    <!-- BİNDİNG GÜNCELLENDİ (Id -> SiraNo) -->
                    <DataGridTextColumn Header="ID" Binding="{Binding SiraNo}" Width="50"/>
                    <DataGridTextColumn Header="Kullanıcı" Binding="{Binding KullaniciAdi}" Width="*"/>
                    <DataGridTextColumn Header="Yetki Tipi" Binding="{Binding YetkiTipi}" Width="*"/>
                    <DataGridTextColumn Header="Modül" Binding="{Binding Modul}" Width="*"/>
                    <DataGridTextColumn Header="Panel" Binding="{Binding Panel}" Width="*"/>
                    <DataGridCheckBoxColumn Header="Yetki" Binding="{Binding YetkiVerildiMi}" Width="60"/>
                    <DataGridTextColumn Header="Diğer Bilgiler" Binding="{Binding DigerBilgiler}" Width="*"/>
                    <DataGridTextColumn Header="Açıklama" Binding="{Binding Aciklama}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>
    </Grid>
</Page>